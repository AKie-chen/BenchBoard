# 2026-09-17

## BenchBoard 项目：初始化 .gitignore
- 工程性质：Qt6 (6.10.2 msvc2022_64) + CMake，生成器 Ninja；同时用 Qt Creator 和 Visual Studio。
- 创建 `E:\BenchBoard\.gitignore`，忽略：`/build*/`（Qt Creator 构建目录）、`/out/`（VS CMake 预设构建目录）、`/.vs/`、`/.qtcreator/`、`CMakeUserPresets.json`、CMake 生成的中间件、Qt moc/qrc/ui 生成物、编译二进制、k6 压测输出（`/results/`、`/logs/`、`*.ndjson`）。
- 有意保留纳入版本控制：`CMakeLists.txt`、`CMakePresets.json`、`src/`、`k6/`、`tests/`。
- 约定：`CMakeUserPresets.json` 默认忽略（含本机 QTDIR 路径与随机 QML 调试端口，Qt Creator 会自动重生成）；若要共享 Debug/Release 预设，改成 `!CMakeUserPresets.json`。
- 验证：`git status --short` 只剩源文件；`git check-ignore` 逐个路径确认无误。
